node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# Raw source media + old render artifacts (large, not project source).
# Kept locally, excluded from git.
_sources-bruts/
preview/
IMCP - IA/

